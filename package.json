{
  "name": "white-label",
  "version": "1.0.0",
  "scripts": {
    "start": "expo start --dev-client",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "build-fox-ios": "rm -rf ios && rm app.json && cp configs/fox.json app.json && npx expo run:ios",
    "build-fox-android": "rm -rf android && rm app.json && cp configs/fox.json app.json && npx expo run:android",
    "build-monkey-ios": "rm -rf ios && rm app.json && cp configs/monkey.json app.json && npx expo run:ios",
    "build-monkey-android": "rm -rf android && rm app.json && cp configs/monkey.json app.json && npx expo run:android",
    "start-fox": "rm app.json && cp configs/fox.json app.json && npx expo start --dev-client",
    "start-monkey": "rm app.json && cp configs/monkey.json app.json && npx expo start --dev-client"
  },
  "dependencies": {
    "expo": "~49.0.8",
    "expo-constants": "~14.4.2",
    "expo-dev-client": "~2.4.8",
    "expo-device": "~5.4.0",
    "expo-notifications": "~0.20.1",
    "expo-splash-screen": "~0.20.5",
    "expo-status-bar": "~1.6.0",
    "expo-system-ui": "~2.4.0",
    "expo-updates": "~0.18.12",
    "react": "18.2.0",
    "react-native": "0.72.4"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  },
  "private": true
}
