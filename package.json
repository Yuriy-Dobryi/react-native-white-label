{
  "name": "automagi-app",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "build-fox-ios": "rm -rf ios && rm app.json && cp configs/fox.json app.json && npx expo prebuild --platform ios --clean",
    "build-fox-android": "rm -rf android && rm app.json && cp configs/fox.json app.json && npx expo prebuild --platform android --clean",
    "build-monkey-ios": "rm -rf ios && rm app.json && cp configs/monkey.json app.json && npx expo prebuild --platform ios --clean",
    "build-monkey-android": "rm -rf android && rm app.json && cp configs/monkey.json app.json && npx expo prebuild --platform android --clean",
    "start-fox": "rm app.json && cp configs/fox.json app.json && npx expo start --dev-client",
    "start-monkey": "rm app.json && cp configs/monkey.json app.json && npx expo start --dev-client"
  },
  "dependencies": {
    "expo": "~49.0.11",
    "expo-status-bar": "~1.6.0",
    "react": "18.2.0",
    "react-native": "0.72.4",
    "expo-notifications": "~0.20.1",
    "expo-device": "~5.4.0",
    "expo-constants": "~14.4.2",
    "expo-splash-screen": "~0.20.5"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@types/react": "~18.2.14",
    "typescript": "^5.1.3"
  },
  "private": true
}
